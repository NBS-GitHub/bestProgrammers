import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header className="app__header">
            <h1 className="app__title">Best Programmers</h1>
        </header>
    );
}

export default Header;